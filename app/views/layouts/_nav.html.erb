<nav class="nav-banner">
  <a class="nav-btn" href="/">Home</a>
  <a class="nav-btn" href="/users">Users</a>
  <a class="nav-btn" href="/groups">Groups</a>

  <% if current_user %>
  	Hi <%= link_to("#{current_user.name}", "/users/#{current_user.id}") %> <%= link_to("Log out", '/logout', method: :delete) %>
  <% else %>
  	<%= link_to 'Log in', '/login' %> <%= link_to 'Sign up', '/users/new' %>
  <% end %>

</nav>
